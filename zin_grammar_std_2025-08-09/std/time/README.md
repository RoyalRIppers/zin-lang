# Module time (std.time) — v0.1

- `fn now_ms() -> num` — timestamp millisecondes depuis l'époque (UTC).
- `fn sleep_ms(num ms)` — alias possible de l'intrinsic `sleep`.
- `fn format_iso(num ms) -> str` — ms -> ISO 8601.
